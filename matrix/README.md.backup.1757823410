# 🧠 Rubin AI Matrix v2.0

## 📋 Описание

Rubin AI Matrix - это модульная система искусственного интеллекта для промышленной автоматизации, состоящая из 5 специализированных узлов, работающих в Docker контейнерах.

## 🏗️ Архитектура

### Узлы системы:

1. **Gateway (Шлюз-Диспетчер)** - Центральный мозг системы
2. **Compute Core (Вычислительное ядро)** - C++ ядро для тяжелых вычислений
3. **PostgreSQL** - Реляционная база данных
4. **Qdrant** - Векторная база данных для семантического поиска
5. **Ollama** - Локальный AI сервис

## 🚀 Быстрый старт

### Предварительные требования:

- Docker и Docker Compose
- 8GB+ RAM
- 20GB+ свободного места на диске
- GPU (опционально, для ускорения AI)

### Установка:

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd rubin-ai-matrix
```

2. **Настройка окружения:**
```bash
cp env_example.txt .env
# Отредактируйте .env файл при необходимости
```

3. **Запуск системы:**
```bash
# Linux/Mac
chmod +x scripts/*.sh
./scripts/start_matrix.sh

# Windows
docker-compose up -d
```

4. **Проверка статуса:**
```bash
./scripts/test_matrix.sh
```

## 🌐 Доступ к сервисам

- **Веб-интерфейс:** http://localhost:8083
- **API документация:** http://localhost:8083/docs
- **Qdrant:** http://localhost:6333
- **Ollama:** http://localhost:11434
- **PostgreSQL:** localhost:5432

## 📊 API Endpoints

### Основные эндпоинты:

- `GET /health` - Проверка здоровья системы
- `GET /api/matrix/status` - Статус всех узлов
- `POST /api/chat` - AI чат
- `POST /api/code/analyze` - Анализ кода
- `POST /api/code/generate` - Генерация кода
- `GET /metrics` - Метрики Prometheus

### Примеры использования:

#### AI Чат:
```bash
curl -X POST "http://localhost:8083/api/chat" \
     -H "Content-Type: application/json" \
     -d '{"message": "Привет, как дела?"}'
```

#### Анализ кода:
```bash
curl -X POST "http://localhost:8083/api/code/analyze" \
     -H "Content-Type: application/json" \
     -d '{
       "code": "print(\"Hello, World!\")",
       "language": "python",
       "analysis_type": "full"
     }'
```

#### Генерация кода:
```bash
curl -X POST "http://localhost:8083/api/code/generate" \
     -H "Content-Type: application/json" \
     -d '{
       "description": "Функция для сортировки массива",
       "language": "python"
     }'
```

## 🔧 Управление системой

### Команды Docker Compose:

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Просмотр логов
docker-compose logs -f

# Перезапуск конкретного сервиса
docker-compose restart gateway

# Обновление сервисов
docker-compose pull
docker-compose up -d
```

### Мониторинг:

```bash
# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# Логи конкретного сервиса
docker-compose logs gateway
docker-compose logs compute_core
docker-compose logs ollama_service
```

## 📈 Масштабирование

### Горизонтальное масштабирование:

```yaml
# docker-compose.scale.yml
services:
  gateway:
    deploy:
      replicas: 3
  
  compute_core:
    deploy:
      replicas: 2
  
  ollama_service:
    deploy:
      replicas: 2
```

### Вертикальное масштабирование:

```yaml
# docker-compose.prod.yml
services:
  gateway:
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
  
  compute_core:
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: 8G
```

## 🛠️ Разработка

### Структура проекта:

```
matrix/
├── gateway_app/          # Gateway приложение
├── cpp_core/            # Compute Core
├── configs/             # Конфигурации
├── scripts/             # Скрипты управления
├── data/                # Данные
│   ├── qdrant/         # Qdrant данные
│   ├── postgres/       # PostgreSQL данные
│   ├── ollama/         # Ollama модели
│   └── logs/           # Логи
├── docker-compose.yml   # Docker Compose
└── README.md           # Документация
```

### Добавление новых узлов:

1. Создайте Dockerfile для нового узла
2. Добавьте сервис в docker-compose.yml
3. Обновите Gateway для взаимодействия с новым узлом
4. Добавьте мониторинг и логирование

## 🔒 Безопасность

### Рекомендации:

1. **Измените пароли по умолчанию** в .env файле
2. **Настройте firewall** для ограничения доступа
3. **Используйте HTTPS** в production
4. **Регулярно обновляйте** Docker образы
5. **Мониторьте логи** на предмет подозрительной активности

### Переменные окружения:

```bash
# Безопасность
JWT_SECRET=your_secure_jwt_secret
API_KEY=your_secure_api_key
POSTGRES_PASSWORD=your_secure_password

# Сеть
GATEWAY_PORT=8083
COMPUTE_CORE_PORT=5000
```

## 📊 Мониторинг и логирование

### Метрики Prometheus:

- `rubin_gateway_requests_total` - Общее количество запросов
- `rubin_gateway_request_duration_seconds` - Время обработки запросов
- `rubin_matrix_tasks_total` - Количество задач матрицы
- `rubin_node_response_time_seconds` - Время ответа узлов

### Логи:

```bash
# Просмотр логов
tail -f data/logs/gateway.log
tail -f data/logs/compute_core.log

# Поиск ошибок
grep -i error data/logs/*.log
```

## 🐛 Устранение неполадок

### Частые проблемы:

1. **Сервис не запускается:**
   - Проверьте логи: `docker-compose logs <service_name>`
   - Убедитесь, что порты не заняты
   - Проверьте доступность ресурсов

2. **Медленная работа:**
   - Увеличьте лимиты памяти в docker-compose.yml
   - Проверьте использование CPU и памяти
   - Оптимизируйте запросы к базам данных

3. **Ошибки подключения:**
   - Проверьте сеть Docker: `docker network ls`
   - Убедитесь, что все сервисы запущены
   - Проверьте настройки firewall

### Полезные команды:

```bash
# Очистка системы
docker system prune -a

# Пересборка образов
docker-compose build --no-cache

# Проверка сети
docker network inspect rubin_matrix_network

# Проверка томов
docker volume ls
```

## 📚 Дополнительные ресурсы

- [Docker документация](https://docs.docker.com/)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Qdrant документация](https://qdrant.tech/documentation/)
- [Ollama документация](https://ollama.ai/docs)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи системы
2. Изучите документацию
3. Создайте issue в репозитории
4. Обратитесь к сообществу

## 📄 Лицензия

MIT License - см. файл LICENSE для подробностей.

---

**Rubin AI Matrix v2.0** - Модульная система ИИ для промышленной автоматизации 🚀
